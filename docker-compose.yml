version: '3'
services:
  # Chỉ định nghĩa 1 khuôn mẫu duy nhất tên là 'web'
  web:
    build: .

  load_balancer:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "8080:80"
    depends_on:
      - web