events { worker_connections 1024; }

http {
    upstream my_web_servers {
        # Chỉ gọi tên nhóm 'web', Docker sẽ tự lo phần còn lại
        server web:3000;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://my_web_servers;
        }
    }
}